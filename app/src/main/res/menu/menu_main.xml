<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">

    <item
        android:id="@+id/menu"
        android:icon="@drawable/ic_menu"
        android:title="menu"
        app:showAsAction="always">
        <menu>
            <item
                android:id="@+id/dashboard"
                android:icon="@drawable/ic_dashboard"
                android:title="Dashboard"
                app:showAsAction="never"></item>
            <item
                android:id="@+id/btn_call_us_menu"
                android:icon="@drawable/ic_call_us_menu"
                android:title="Call Us"
                app:showAsAction="never"></item>
            <item
                android:id="@+id/btn_arrange_call_menu"
                android:icon="@drawable/ic_arrange_call_menu"
                android:title="Arrange Call"
                app:showAsAction="never"></item>
            <item
                android:id="@+id/btn_raise_complaint_menu"
                android:icon="@drawable/ic_raise_complain_menu"
                android:title="Raise Request"
                app:showAsAction="never"></item>
            <item
                android:id="@+id/btn_complaint_menu"
                android:icon="@drawable/ic_complaint_menu"
                android:title="Service Request"
                app:showAsAction="never"></item>
            <item
                android:id="@+id/btn_machines_menu"
                android:icon="@drawable/ic_machine_menu"
                android:title="Machine"
                app:showAsAction="never"></item>
            <item
                android:id="@+id/btn_feedback_menu"
                android:icon="@drawable/ic_feedback_menu"
                android:title="FeedBack"
                app:showAsAction="never"></item>
            <item
                android:id="@+id/profile"
                android:icon="@drawable/ic_profile_menu"
                android:title="Profile"
                app:showAsAction="never"></item>
            <item
                android:id="@+id/change_password"
                android:icon="@drawable/ic_changepassword_menu"
                android:title="Change Password"
                app:showAsAction="never"></item>

            <item
                android:id="@+id/logout"
                android:icon="@drawable/ic_logout_menu"
                android:title="Logout"
                app:showAsAction="never"></item>
        </menu>
    </item>
</menu>